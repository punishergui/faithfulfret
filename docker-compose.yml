services:
  daily-fret:
    build: .
    container_name: daily-fret
    restart: unless-stopped
    ports:
      - "9999:9999"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - PORT=9999
      - NODE_ENV=production
