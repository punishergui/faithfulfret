services:
  daily-fret:
    build: .
    container_name: daily-fret
    restart: unless-stopped
    ports:
      - "3000:9999"
    volumes:
      - ./data:/data
      - .:/app
      - /app/node_modules
    environment:
      - PORT=9999
      - NODE_ENV=production
